# Public endpoint (not a secret) — the Worker itself holds the Cloudflare TURN
# credentials and CORS-locks responses to the Pages origin.
VITE_TURN_ENDPOINT=https://tablestakes-turn.joshuarlowry.workers.dev
