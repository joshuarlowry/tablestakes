name = "tablestakes-turn"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Origins allowed to request TURN credentials. Comma-separated.
# Add your local dev origin here while testing.
[vars]
ALLOWED_ORIGINS = "https://joshuarlowry.github.io,http://localhost:5173"
CREDENTIAL_TTL = "86400"

# Secrets are NOT stored here. Set them out-of-band:
#   wrangler secret put TURN_KEY_ID
#   wrangler secret put TURN_KEY_API_TOKEN
# (CI sets these automatically from GitHub secrets — see the deploy workflow.)

# Room relay Durable Object — one instance per game room, gossips opaque `evt`
# envelopes between connected clients (see src/room.js). Free plan requires
# `new_sqlite_classes` (not `new_classes`); a prior smoke-tested-then-deleted
# class already consumed migration tags v1/v2, hence starting at v3 here.
[[durable_objects.bindings]]
name = "ROOM_DO"
class_name = "RoomDO"

[[migrations]]
tag = "v2"
deleted_classes = ["RoomDO"]

[[migrations]]
tag = "v3"
new_sqlite_classes = ["RoomDO"]
